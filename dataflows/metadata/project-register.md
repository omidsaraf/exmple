# Project Register - GENERATED INDEX (do not hand-edit)

> ADR-50 R3: this file is REGENERATED from `metadata/*/manifest.yml` by
> `new_dataflow.py` (and the `--reindex` flag). It is an audit index, NOT the issuing
> authority. The `project_code` is a STABLE EXTERNAL KEY supplied at scaffold time
> (HEARING_P{n} or a delivery-system id like GDDDCI629); uniqueness is enforced by the
> filesystem (no-clobber on the project folder), so concurrent scaffolds never collide on
> a shared mutable file. The domain lives in the `dag_uid`, never in the code.

| project_code | dag_uid | domain | kind | owner |
|---|---|---|---|---|
| HEARING_P1 | df_audiology_appointments | audiology | mixed | fabric-admin@Samestantil44outlook.onmicrosoft.com |
| HEARING_P2 | df_dynamics_contact | dynamics | mixed | fabric-admin@Samestantil44outlook.onmicrosoft.com |
| HEARING_P3 | df_audiology_devicetelemetry | audiology | ingestion | fabric-admin@Samestantil44outlook.onmicrosoft.com |
| HEARING_P4 | df_audiology_archival | audiology | ingestion | - |
